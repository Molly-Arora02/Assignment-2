{
  "version": 2,
  "buildCommand": "cd client && npm install && npm run build && cd .. && rm -rf dist && cp -r client/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "/api/index.js"
    },
    {
      "source": "/:match*",
      "destination": "/index.html"
    }
  ]
}
