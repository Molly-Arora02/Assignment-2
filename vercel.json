{
  "version": 2,
  "buildCommand": "if [ -d \"client\" ]; then cd client && npm install --include=dev && npm run build && cd .. && rm -rf dist && cp -r client/dist dist; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "/api/index.js"
    },
    {
      "source": "/:match*",
      "destination": "/index.html"
    }
  ]
}
